# Secrets / local environment — never commit
.env
*.key

# Scraper output — keep your job-search data private (uploaded as an
# Actions artifact instead, see the workflow)
job_tracker.xlsx
*.xlsx

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS / editor cruft
.DS_Store
*.swp
